<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <%= link_to "Life Blog", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse mr-auto" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <% if request.path == root_path || request.path == posts_path %>
                        <%= link_to 'Posts', root_path, class: "nav-link active" %>
                    <% else %>
                        <%= link_to 'Posts', root_path, class: "nav-link" %>
                    <% end %>
                </li>
                <li class="nav-item">
                    <% if request.path == new_post_path %>
                        <%= link_to 'New post', new_post_path, class: "nav-link active" %>
                    <% else %>
                        <%= link_to 'New post', new_post_path, class: "nav-link" %>
                    <% end %>
                </li>
            </ul>
        </div>
    </div>
</nav>